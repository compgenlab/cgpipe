# Outputs generated by running the examples in place (check.sh uses temp dirs,
# but a user running an example in its directory will create these).
01-hello/hello.txt
02-batch-compress/data/*.gz
03-scatter-gather/*.count
03-scatter-gather/total.txt
04-manifest-cohort/*.sum
05-stage-workflow/clean.txt
05-stage-workflow/summary.txt
06-cluster-resources/result.txt
