#!/usr/bin/env cgpipe
#
# The smallest pipeline: build one file.
#
# Options:
#     --name NAME   who to greet (default: world)
#
# Run:  cgpipe pipeline.cgp | bash   (then: cat hello.txt)

name ?= "world"

hello.txt: {{
    echo "Hello, ${name}!" > ${output}
}}
@default: hello.txt
