# §8 @setup runs first, @teardown last; shexec bodies still render under -dr.
@setup {{
    job.shexec = true
    --
    mkdir -p logs
}}
@teardown {{
    job.shexec = true
    --
    echo "pipeline complete"
}}
out.txt: {{
    echo hi > ${output}
}}
@default: out.txt
