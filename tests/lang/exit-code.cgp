# §5.2 exit <code> stops the pipeline and propagates the code as cgp's exit
# status; statements after exit do not run. (Expected rc is in exit-code.cgp.rc.)
print "before exit"
exit 3
print "after exit (never printed)"
