# §5.1 `for <var> in <iterable> with <i>` binds a 1-based loop counter alongside
# the element. Works over lists and ranges; both vars persist after the loop.
for s in ["a", "b", "c"] with i { print i, s }
for x in 10..12 with n { print n, x }
print "last", i, s
exit
