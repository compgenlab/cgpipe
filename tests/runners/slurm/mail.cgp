# §11.4 Setting mail adds the scheduler's notification directives, defaulting
# the mail type when unset (SLURM END,FAIL).
out.bam: {{
    job.name = "j"
    job.mail = "user@example.com"
    --
    echo x > ${output}
}}
@default: out.bam
