# §10.4 Restart is mtime-based: an output newer than its input is current, so
# nothing is submitted (no stdout, no scheduler calls). setup.sh sets the mtimes.
out.bam: in.fastq {{
    job.name = "align"
    --
    echo x > ${output}
}}
@default: out.bam
